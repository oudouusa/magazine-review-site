{
  "name": "magazine-review-site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && npx pagefind --site \"$([ -d dist/client ] && echo dist/client || echo dist)\"",
    "preview": "astro preview",
    "astro": "astro",
    "generate-data": "python scripts/generate-data.py"
  },
  "dependencies": {
    "@astrojs/mdx": "^5.0.0",
    "@astrojs/sitemap": "^3.0.0",
    "@tailwindcss/typography": "^0.5.19",
    "@tailwindcss/vite": "^4.2.2",
    "astro": "^6.1.7",
    "tailwindcss": "^4.2.2"
  },
  "devDependencies": {
    "pagefind": "^1.5.2"
  }
}
